(function(global) {
    
    // TODO: not the way to do auth in a web app
    
    global.firebaseConfig = Object.freeze({
        "NAME": "https://ADD-FIREBASE-NAME-HERE.firebaseIO.com/",
        "AUTH_TOKEN": "ADD-TOKEN-HERE",
    });

}(window));
